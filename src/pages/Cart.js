import EmptyCart from "../components/cart/EmptyCart";
import CartItems from "../components/cart/CartItems";
const Cart = () => {
  return (
    <div>
      <EmptyCart />
      <CartItems />
    </div>
  );
};

export default Cart;
